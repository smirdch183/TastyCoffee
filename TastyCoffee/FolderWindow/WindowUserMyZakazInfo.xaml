<Window x:Class="TastyCoffee.FolderWindow.WindowUserMyZakazInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TastyCoffee.FolderWindow"
        mc:Ignorable="d"
        Title="Информация о заказе" WindowStartupLocation="CenterScreen"
        Window.SizeToContent="WidthAndHeight" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
            <RowDefinition/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <Image Style="{DynamicResource Logo}" Width="100" Grid.Column="1" Grid.Row="1"/>
        <Label Grid.Column="2" Grid.Row="1" Content="Информация о заказе" Style="{DynamicResource LogoLabel}"/>
        <DataGrid x:Name="DgZakaz" Grid.Row="3" Grid.ColumnSpan="2" Grid.Column="1" Grid.RowSpan="3"/>
    </Grid>
</Window>
